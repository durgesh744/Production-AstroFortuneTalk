import {View, Text} from 'react-native';
import React from 'react';

const UploadFreePdf = ({updateState}) => {
  return (
    <View>
      <Text>UploadFreePdf</Text>
    </View>
  );
};

export default UploadFreePdf;
